# DentAI Flow — Frontend local development overrides
# Bu dosya Git'e commit edilmez (.gitignore'da olmalı)

# API Gateway URL — tarayıcı bu adrese istek atar
NEXT_PUBLIC_API_URL=http://localhost:8081/api
