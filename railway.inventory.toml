# Inventory Railway servisi → Config-as-code path: railway.inventory.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "services/inventory-service/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
